replicaCount: 1

image:
  repository: ralucazaicanu/task-manager
  tag: latest

containerPort: 8080

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: traefik
  host: task-manager.local